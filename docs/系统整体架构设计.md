# AI-DocAgent 整体架构设计文档
## 1.架构分层
整体分为五层：数据源接入层、文档解析层、AI抽取处理层、数据仓储层、任务调度管理层
1. **数据源接入层**：支持本地文件、远程文件URL批量拉取，内置通用爬虫组件，配置UA池、请求重试熔断机制解决第三方接口限流、页面访问不稳定问题
2. **文档解析层**：多类型文件解析，提取正文、表格、内嵌图片元素，原始数据落库MongoDB
3. **AI抽取处理层**：通过定制场景化Prompt调用通用LLM API，完成非结构化文本关键信息抽取
4. **结构化&质检层**：统一转换为标准JSON格式，通过字段规则+模型双校验完成数据质检、MD5全局去重、脏数据过滤
5. **调度管理层**：基于自定义状态配置文件+Redis实现批次管理、任务状态监控、断点续传、批量启停调度；MySQL持久化全量任务台账

## 2.数据流
原始文档资源 → 文档解析 → 原始文本缓存 → LLM结构化抽取 → 格式标准化 → 多维度质检 → 合规数据入库 → 任务状态归档

## 3.部署架构
基于Linux服务器进行项目部署，Shell脚本统一管控定时任务、批量作业启停，支持单机/分布式任务拓展。
